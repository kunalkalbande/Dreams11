import { PlayerFilterByRolePipe } from './player-filter-by-role.pipe';

describe('PlayerFilterByRolePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerFilterByRolePipe();
    expect(pipe).toBeTruthy();
  });
});
